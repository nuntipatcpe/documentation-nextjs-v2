# Css in line

- Styles

    ```
    const styles = {
        container : {
        backgroundColor: "#f0f",
        height: 200  
        }
    }
    ```

- Usage

  ```
  return(
    <div style={ styles.container }> </div>
  )
  ```
